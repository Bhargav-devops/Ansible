- name: Install nginx and run nginx
  hosts: roboshop
  become: yes
  tasks:
    - name: instal nginx
      ansible.builtin.package:
        name: nginx
        state: present
    - name: Run nginx
      ansible.builtin.service:
        name: nginx
        state: started
