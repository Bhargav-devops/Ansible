#playbook is list of plays
- name: play-1
  hosts: localhost
  tasks:
    - name: play-1 and task-1
      ansible.builtin.debug:
        msg: "this is play-1 ans task1"

- name: play 2
  hosts: localhost
  tasks:
    - name: play 2 and task2
      ansible.builtin.debug:
        msg: play-2 and task-2
